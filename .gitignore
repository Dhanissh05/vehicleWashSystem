node_modules
dist
.env
.env.local
*.log
.DS_Store
.vscode
coverage
*.sqlite
.expo

# Firebase credentials (NEVER commit the backend service account)
backend/firebase-service-account.json
*service-account*.json
.expo-shared
build

# Keep .env.example files
!.env.example

# Database files
*.db

# HTML strategy files
PRICING_STRATEGY.html

# Backend runtime build is required in this repo (backend/src is a shim)
!backend/dist/
!backend/dist/**

# Documentation files - setup guides
PUSH_NOTIFICATION_FIX.md
SOUND_NOTIFICATION_SETUP.md
**/SOUND_SETUP.md
