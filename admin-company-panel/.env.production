# Production API URL — gets baked into the admin panel build by Vite.
# Override via Docker build arg: --build-arg VITE_API_URL=https://your-backend.up.railway.app/graphql
# If the admin panel is served from the same host as the backend, use a relative path.
VITE_API_URL=/graphql
